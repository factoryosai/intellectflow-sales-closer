# Intellect Flow - AI Sales Agent
Stack: Next.js + Supabase + Gemini + Places API + Evolution API (Railway)

Setup:
1. Supabase table `leads` banao (id uuid, business_name text, niche text, address text, phone text, rating float, review_count int, pain_point text, suggested_plan text, whatsapp_message text, status text)
2. .env.local me keys dalo
3. npm install && npm run dev
